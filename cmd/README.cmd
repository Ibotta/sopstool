# cmd

Light wrappers around the basic sopstool package commands using the [cobra](https://github.com/spf13/cobra) package

This package, by itself, isn't that useful - the `../main.go` requires this.
